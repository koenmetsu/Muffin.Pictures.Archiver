<?xml version="1.0" encoding="utf-8" ?>
<smtp from="#{smtp.from}">
  <network
       host="#{smtp.host}" port="#{smtp.port}"
       userName="#{smtp.userName}" password="#{smtp.password}" enableSsl="#{smtp.enableSsl}" />
</smtp>
